import 'package:flutter/material.dart';

text(text, double size, FontWeight) {
  return Text(text,
      style: TextStyle(
          fontSize: size, fontWeight: FontWeight, color: Color(0xff0D986A)));
}

text1(text, double size, FontWeight) {
  return Text(text,
      style: TextStyle(
        fontSize: size,
        fontWeight: FontWeight,
        color: Colors.black,
      ));
}
